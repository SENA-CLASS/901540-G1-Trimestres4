/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package edu.co.sena.ejemplodao.modelo.jdbc;

import edu.co.sena.ejemplodao.modelo.dao.*;
import edu.co.sena.ejemplodao.modelo.factory.*;
import edu.co.sena.ejemplodao.modelo.dto.*;
import edu.co.sena.ejemplodao.modelo.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class CarritoComprasDaoImpl extends AbstractDAO implements CarritoComprasDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_CARRITO, TOTAL, SUBTOTAL, IMPUESTOS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_CARRITO, TOTAL, SUBTOTAL, IMPUESTOS ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_CARRITO = ?, TOTAL = ?, SUBTOTAL = ?, IMPUESTOS = ? WHERE ID_CARRITO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_CARRITO = ?";

	/** 
	 * Index of column ID_CARRITO
	 */
	protected static final int COLUMN_ID_CARRITO = 1;

	/** 
	 * Index of column TOTAL
	 */
	protected static final int COLUMN_TOTAL = 2;

	/** 
	 * Index of column SUBTOTAL
	 */
	protected static final int COLUMN_SUBTOTAL = 3;

	/** 
	 * Index of column IMPUESTOS
	 */
	protected static final int COLUMN_IMPUESTOS = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_CARRITO
	 */
	protected static final int PK_COLUMN_ID_CARRITO = 1;

	/** 
	 * Inserts a new row in the carrito_compras table.
	 */
	public CarritoComprasPk insert(CarritoCompras dto) throws CarritoComprasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getIdCarrito() );
			stmt.setDouble( index++, dto.getTotal() );
			stmt.setDouble( index++, dto.getSubtotal() );
			stmt.setDouble( index++, dto.getImpuestos() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CarritoComprasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the carrito_compras table.
	 */
	public void update(CarritoComprasPk pk, CarritoCompras dto) throws CarritoComprasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getIdCarrito() );
			stmt.setDouble( index++, dto.getTotal() );
			stmt.setDouble( index++, dto.getSubtotal() );
			stmt.setDouble( index++, dto.getImpuestos() );
			stmt.setString( 5, pk.getIdCarrito() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CarritoComprasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the carrito_compras table.
	 */
	public void delete(CarritoComprasPk pk) throws CarritoComprasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getIdCarrito() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CarritoComprasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the carrito_compras table that matches the specified primary-key value.
	 */
	public CarritoCompras findByPrimaryKey(CarritoComprasPk pk) throws CarritoComprasDaoException
	{
		return findByPrimaryKey( pk.getIdCarrito() );
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria 'ID_CARRITO = :idCarrito'.
	 */
	public CarritoCompras findByPrimaryKey(String idCarrito) throws CarritoComprasDaoException
	{
		CarritoCompras ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_CARRITO = ?", new Object[] { idCarrito } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria ''.
	 */
	public CarritoCompras[] findAll() throws CarritoComprasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_CARRITO", null );
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria 'ID_CARRITO = :idCarrito'.
	 */
	public CarritoCompras[] findWhereIdCarritoEquals(String idCarrito) throws CarritoComprasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_CARRITO = ? ORDER BY ID_CARRITO", new Object[] { idCarrito } );
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria 'TOTAL = :total'.
	 */
	public CarritoCompras[] findWhereTotalEquals(double total) throws CarritoComprasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TOTAL = ? ORDER BY TOTAL", new Object[] {  new Double(total) } );
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria 'SUBTOTAL = :subtotal'.
	 */
	public CarritoCompras[] findWhereSubtotalEquals(double subtotal) throws CarritoComprasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE SUBTOTAL = ? ORDER BY SUBTOTAL", new Object[] {  new Double(subtotal) } );
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the criteria 'IMPUESTOS = :impuestos'.
	 */
	public CarritoCompras[] findWhereImpuestosEquals(double impuestos) throws CarritoComprasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IMPUESTOS = ? ORDER BY IMPUESTOS", new Object[] {  new Double(impuestos) } );
	}

	/**
	 * Method 'CarritoComprasDaoImpl'
	 * 
	 */
	public CarritoComprasDaoImpl()
	{
	}

	/**
	 * Method 'CarritoComprasDaoImpl'
	 * 
	 * @param userConn
	 */
	public CarritoComprasDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "tienda_online2.carrito_compras";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected CarritoCompras fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			CarritoCompras dto = new CarritoCompras();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected CarritoCompras[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			CarritoCompras dto = new CarritoCompras();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		CarritoCompras ret[] = new CarritoCompras[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(CarritoCompras dto, ResultSet rs) throws SQLException
	{
		dto.setIdCarrito( rs.getString( COLUMN_ID_CARRITO ) );
		dto.setTotal( rs.getDouble( COLUMN_TOTAL ) );
		dto.setSubtotal( rs.getDouble( COLUMN_SUBTOTAL ) );
		dto.setImpuestos( rs.getDouble( COLUMN_IMPUESTOS ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(CarritoCompras dto)
	{
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the specified arbitrary SQL statement
	 */
	public CarritoCompras[] findByDynamicSelect(String sql, Object[] sqlParams) throws CarritoComprasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CarritoComprasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the carrito_compras table that match the specified arbitrary SQL statement
	 */
	public CarritoCompras[] findByDynamicWhere(String sql, Object[] sqlParams) throws CarritoComprasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CarritoComprasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
